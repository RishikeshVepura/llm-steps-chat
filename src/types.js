/**
 * @typedef {Object} Step
 * @property {string} description - Human-readable step text
 * @property {string} [image] - Optional image URL or base64 string
 * @property {boolean} done - false on creation; true after user marks it done
 */

/**
 * @typedef {Step[]} StepList
 * An ordered list of steps. Index 0 is the first step to be shown.
 */

export {}
